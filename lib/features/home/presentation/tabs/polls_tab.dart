import 'package:flutter/material.dart';

class PollsTab extends StatelessWidget {
  const PollsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Опитування — скоро тут'),
    );
  }
}
